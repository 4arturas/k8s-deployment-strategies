apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus            # fixed UID so dashboards can reference it
    type: prometheus
    access: proxy
    isDefault: true
    url: http://prometheus:9090
    jsonData:
      httpMethod: POST